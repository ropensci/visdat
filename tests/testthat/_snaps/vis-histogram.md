# vis_histogram sends an error when used with the wrong data

    Data input can only contain numeric values
    Please subset the data to the numeric values you would like.
    `dplyr::select(<data>, where(is.numeric))`
    Can be helpful here!

